name: Public Web Image

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "apps/public-web/**"
      - "packages/frontend/**"
      - "package.json"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - "turbo.json"
      - ".github/workflows/public-web-image.yml"

permissions:
  contents: read
  packages: write

concurrency:
  group: public-web-image-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    env:
      IMAGE_NAME: ghcr.io/netmetric/public-web
      NEXT_PUBLIC_SITE_URL: https://netmetric.net
      NEXT_PUBLIC_AUTH_URL: https://auth.netmetric.net
      NEXT_PUBLIC_ACCOUNT_URL: https://account.netmetric.net
      NEXT_PUBLIC_CRM_URL: https://crm.netmetric.net
      NEXT_PUBLIC_TOOLS_URL: https://tools.netmetric.net
      NEXT_PUBLIC_API_URL: https://api.netmetric.net

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/public-web/Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:${{ github.sha }}
          build-args: |
            NEXT_PUBLIC_SITE_URL=${{ env.NEXT_PUBLIC_SITE_URL }}
            NEXT_PUBLIC_AUTH_URL=${{ env.NEXT_PUBLIC_AUTH_URL }}
            NEXT_PUBLIC_ACCOUNT_URL=${{ env.NEXT_PUBLIC_ACCOUNT_URL }}
            NEXT_PUBLIC_CRM_URL=${{ env.NEXT_PUBLIC_CRM_URL }}
            NEXT_PUBLIC_TOOLS_URL=${{ env.NEXT_PUBLIC_TOOLS_URL }}
            NEXT_PUBLIC_API_URL=${{ env.NEXT_PUBLIC_API_URL }}
