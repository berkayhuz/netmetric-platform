name: CRM Web Image

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "apps/crm-web/**"
      - "packages/frontend/ui/**"
      - "packages/frontend/i18n/**"
      - "package.json"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - "turbo.json"
      - ".github/workflows/crm-web-image.yml"

permissions:
  contents: read
  packages: write

concurrency:
  group: crm-web-image-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    env:
      IMAGE_NAME: ghcr.io/netmetric/crm-web
      NEXT_PUBLIC_APP_NAME: NetMetric CRM
      NEXT_PUBLIC_APP_ORIGIN: https://crm.netmetric.net
      NEXT_PUBLIC_CRM_URL: https://crm.netmetric.net
      NEXT_PUBLIC_AUTH_URL: https://auth.netmetric.net
      NEXT_PUBLIC_API_BASE_URL: https://api.netmetric.net

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: apps/crm-web/Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:${{ github.sha }}
          build-args: |
            NEXT_PUBLIC_APP_NAME=${{ env.NEXT_PUBLIC_APP_NAME }}
            NEXT_PUBLIC_APP_ORIGIN=${{ env.NEXT_PUBLIC_APP_ORIGIN }}
            NEXT_PUBLIC_CRM_URL=${{ env.NEXT_PUBLIC_CRM_URL }}
            NEXT_PUBLIC_AUTH_URL=${{ env.NEXT_PUBLIC_AUTH_URL }}
            NEXT_PUBLIC_API_BASE_URL=${{ env.NEXT_PUBLIC_API_BASE_URL }}
