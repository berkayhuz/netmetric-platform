apiVersion: v1
kind: Service
metadata:
  name: account-web
  namespace: netmetric
  labels:
    app.kubernetes.io/name: account-web
    app.kubernetes.io/part-of: netmetric
spec:
  selector:
    app.kubernetes.io/name: account-web
  ports:
    - name: http
      port: 80
      targetPort: http
