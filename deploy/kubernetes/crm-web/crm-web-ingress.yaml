apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: crm-web
  namespace: netmetric
  labels:
    app.kubernetes.io/name: crm-web
    app.kubernetes.io/part-of: netmetric
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - crm.netmetric.net
      secretName: crm-web-netmetric-tls
  rules:
    - host: crm.netmetric.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: crm-web
                port:
                  number: 80
