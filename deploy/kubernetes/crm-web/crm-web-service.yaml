apiVersion: v1
kind: Service
metadata:
  name: crm-web
  namespace: netmetric
  labels:
    app.kubernetes.io/name: crm-web
    app.kubernetes.io/part-of: netmetric
spec:
  selector:
    app.kubernetes.io/name: crm-web
  ports:
    - name: http
      port: 80
      targetPort: http
