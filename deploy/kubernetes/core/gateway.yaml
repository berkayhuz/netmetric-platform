apiVersion: v1
kind: ConfigMap
metadata:
  name: gateway-env
  namespace: netmetric
data:
  ASPNETCORE_ENVIRONMENT: "Production"
  ASPNETCORE_URLS: "http://+:8080"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway
  namespace: netmetric
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: gateway } }
  template:
    metadata: { labels: { app.kubernetes.io/name: gateway, app.kubernetes.io/part-of: netmetric } }
    spec:
      serviceAccountName: gateway
      containers:
        - name: gateway
          image: ghcr.io/netmetric/gateway:${IMAGE_TAG}
          ports: [{ name: http, containerPort: 8080 }]
          envFrom:
            [{ configMapRef: { name: gateway-env } }, { secretRef: { name: gateway-secrets } }]
          readinessProbe:
            {
              httpGet: { path: /health/ready, port: http },
              initialDelaySeconds: 10,
              periodSeconds: 10,
            }
          livenessProbe:
            {
              httpGet: { path: /health/live, port: http },
              initialDelaySeconds: 20,
              periodSeconds: 20,
            }
          securityContext:
            {
              allowPrivilegeEscalation: false,
              readOnlyRootFilesystem: true,
              capabilities: { drop: ["ALL"] },
              runAsNonRoot: true,
              seccompProfile: { type: RuntimeDefault },
            }
          resources:
            { requests: { cpu: 150m, memory: 256Mi }, limits: { cpu: 800m, memory: 768Mi } }
---
apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: netmetric
spec:
  selector: { app.kubernetes.io/name: gateway }
  ports: [{ name: http, port: 8080, targetPort: http }]
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: gateway
  namespace: netmetric
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: gateway
  namespace: netmetric
spec:
  minAvailable: 1
  selector: { matchLabels: { app.kubernetes.io/name: gateway } }
