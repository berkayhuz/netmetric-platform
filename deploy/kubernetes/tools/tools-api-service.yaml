apiVersion: v1
kind: Service
metadata:
  name: tools-api
  namespace: netmetric
  labels:
    app.kubernetes.io/name: tools-api
    app.kubernetes.io/part-of: netmetric
spec:
  selector:
    app.kubernetes.io/name: tools-api
  ports:
    - name: http
      port: 80
      targetPort: http
