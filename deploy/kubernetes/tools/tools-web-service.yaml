apiVersion: v1
kind: Service
metadata:
  name: tools-web
  namespace: netmetric
  labels:
    app.kubernetes.io/name: tools-web
    app.kubernetes.io/part-of: netmetric
spec:
  selector:
    app.kubernetes.io/name: tools-web
  ports:
    - name: http
      port: 80
      targetPort: http
