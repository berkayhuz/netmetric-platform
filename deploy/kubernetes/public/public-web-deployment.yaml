apiVersion: apps/v1
kind: Deployment
metadata:
  name: public-web
  namespace: netmetric
  labels:
    app.kubernetes.io/name: public-web
    app.kubernetes.io/part-of: netmetric
spec:
  replicas: 2
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app.kubernetes.io/name: public-web
  template:
    metadata:
      labels:
        app.kubernetes.io/name: public-web
        app.kubernetes.io/part-of: netmetric
    spec:
      serviceAccountName: public-web
      volumes:
        - name: tmp
          emptyDir: {}
      containers:
        - name: public-web
          image: ghcr.io/netmetric/public-web:${IMAGE_TAG}
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 7003
          envFrom:
            - configMapRef:
                name: public-web-env
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
            seccompProfile:
              type: RuntimeDefault
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
